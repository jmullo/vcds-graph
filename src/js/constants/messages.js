export const ALREADY_IMPORTED = (fileName) => `"${fileName}" is already imported.`;

export const NO_MEASUREMENTS = (fileName) => `"${fileName}" contains no measurements.`;

export const UNABLE_TO_IMPORT = (fileName) => 
    `Unable to import "${fileName}". Try another log file or contact: jussi.mullo@iki.fi`;
